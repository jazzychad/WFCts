export enum AdjacencyDirection {
    north,
    east,
    south,
    west
}

export enum FlipDirection {
    horrizontal,
    veritcal
}

export enum PropagationResult {
    success,
    failure
}

export type AdjacencyIdentifer = string
export type SocketIdentifier = string